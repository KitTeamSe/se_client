const textEllipsis = `
    text-overflow: ellipsis;
    overflow: hidden;
    white-space: nowrap;
`;

const flexCenter = `
    display: flex;
    justify-content: center;
    align-items: center;
`;

export default { textEllipsis, flexCenter };
